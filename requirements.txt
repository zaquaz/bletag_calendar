# Core dependencies for BLE E-ink tag project
# Image processing
Pillow>=9.0.0

# Bluetooth Low Energy
bleak>=0.19.0

# Numerical computing
numpy>=1.21.0

# HTTP requests
requests>=2.28.0

# Calendar parsing
icalendar>=4.1.0

# Standard library packages that might need explicit installation in some environments
# (most of these are included with Python but listing for completeness)
# asyncio - built-in
# argparse - built-in  
# sys - built-in
# math - built-in
# pathlib - built-in
# logging - built-in
# enum - built-in
# datetime - built-in
# typing - built-in
# signal - built-in
